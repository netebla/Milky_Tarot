version: "3.9"
services:
  bot:
    build: .
    container_name: tarot_bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - TZ=${TZ:-Europe/Moscow}
    volumes:
      - ./src/data:/app/src/data
    restart: unless-stopped 